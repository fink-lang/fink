

# A function whose body does an irrefutable destructure that fails at
# runtime when called with a value lacking the field. Used to pin that
# trap locations resolve to the dependency module's source line, not the
# entry module's line 1.
boom = fn rec:
  {missing} = rec
  missing
