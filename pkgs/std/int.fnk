# The built-in `int` type, reified as a first-class value: usable as a
# match/guard arm (`match x: int i: ...`). Matches any integer ($I64 / $U64).
{IntType: rt_int_type} = import 'rt/int.wat'

int = rt_int_type
